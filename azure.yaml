# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: cv
metadata:
  template: cv@0.0.1-beta
services:
  app:
    project: ./web
    language: ts
    host: containerapp
    docker:
      path: ./Dockerfile
      context: ../
infra:
  provider: bicep
